# Optional override. This is also the built-in local default.
DATABASE_URL="file:./.data/firstaid.db"

# Optional managed-document storage override. Keep this directory private.
DOCUMENTS_DIR="./.data/documents"
