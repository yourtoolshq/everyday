#!/bin/sh
set -eu

cd /app/apps/passbook
if [ "$(id -u)" = "0" ]; then
  exec su-exec nextjs node scripts/yt-data.cjs --app passbook --migrations drizzle "$@"
fi
exec node scripts/yt-data.cjs --app passbook --migrations drizzle "$@"
